CREATE TABLE IF NOT EXISTS people (
    index integer,
    userId text,
    firstName text,
    lastName text,
    sex text,
    email text,
    phone text,
    dateOfBirth DATE,
    jobTitle text
);
